## 1. Local runtime

- [x] 1.1 Add a local Compose model for frontend, backend, worker and PostgreSQL.
- [x] 1.2 Add safe local environment defaults and optional ignored provider-token override.
- [x] 1.3 Add health/readiness dependencies, loopback port bindings and persistent local data.

## 2. Developer workflow

- [x] 2.1 Add root commands for local start, stop, status, logs and explicit data reset.
- [x] 2.2 Document setup, manual smoke tests, provider testing and troubleshooting.
- [x] 2.3 Update persistent AI project context with the local runtime contract.

## 3. Validation

- [x] 3.1 Resolve the local Compose model with safe defaults and validate repository quality gates.
- [x] 3.2 Build, start and health-check the complete local stack, then stop it without deleting data.
- [x] 3.3 Run strict OpenSpec validation after implementation.
